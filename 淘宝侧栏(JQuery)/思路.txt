1.先获取到所有div与窗口顶部的距离
2.获取到当前滚动条的距离
3.判断滚动条在div哪个区间(利用遍历循环+判断scrolltop>offset)，并赋值到一个空变量上
4.判断赋值变量是否为空（为空就说明在默认第一个区间内，不需要更改），判断遍历出来的区间，和当前区间是否相同（相同也不需要改）
5.当4都不成立的时候，使用removeClass（）将样式删除
6.addClass()将样式添加到新区间中

