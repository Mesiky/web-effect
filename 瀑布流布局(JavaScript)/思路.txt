1.获取屏幕宽度；
2.屏幕/图片width 获取每行能放置个数
3.获取每个div的offset高度和宽度
4.将第一行的所有高度放入一个数组，并获取到最低的一个；
5.获取到最小一个的标识号；
6.将第一行后面都绝对定位；
7.将最低的高度+新定位的offsetTop；就不会是最小高度；
8.最小值和标识都是动态的;
9.使用onscroll事件，判断滚动条+可视区（因为网页大于可视区才会有滚动条）》最后一个div相对高度+他本身一般的高度
10.9成立后就执行创建元素，为元素添加属性，将属性附加到指定区域
11.重新判断所以div的数量（因为新增了div元素）
12.将数组重新归为第一行的高度（此方式不合理，暂用）
（因为在没有滚动条事件的时候，第一行下面都已经定位过了，但是这里又全部进行了全部重新定位，造成重复定位）
13.将所有的div（除第一行）重新定位
